# Default PyTorch on Linux is CUDA (multi‑GB NVIDIA wheels). Pin CPU builds for servers
# without a GPU (e.g. Render). Local dev uses the same wheels from this index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
groq==0.28.0
httpx==0.27.0
sentence-transformers==3.0.1
faiss-cpu==1.13.2
numpy==1.26.4
python-dotenv==1.0.1
